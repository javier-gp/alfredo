var request = require('supertest'),
    index = require('../routes'),
    app = require('../app.js');

describe('Videos list', function (){
    it('should return an array with 10 items', function(done){
    });
    it('should have a video object inside each item', function(done){
    });
    it('should have a title per item', function(done){
    });
    it('should have a link per item', function(done){
    });
});
